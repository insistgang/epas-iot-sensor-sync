# Code

`generate_polished_figures.py` regenerates the polished scientific figures from the processed values embedded in the script.

The expanded executable simulation engine and raw run logs are available from the corresponding author upon reasonable request. The public package provides processed values, configuration notes, random-seed information, and figure-generation assets for manuscript verification.
